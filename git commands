# Step 1: Initialize Git in your project folder
git init

# Step 2: Add the HTML file to the staging area
git add index.html

# Step 3: Commit the file to the local repository
git commit -m "Initial commit with simple HTML file"

# Step 4: Create a repository on GitHub and link it
git remote add origin https://github.com/yourusername/version-control-example.git

# Step 5: Push the changes to the GitHub repository
git push -u origin master

# Optional: Set up caching of GitHub login credentials
git config --global credential.helper cache
